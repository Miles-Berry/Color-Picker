// filename: layers.component.js
// author: Miles-Berry

export default function Layers() {
    return (
    <div>

        {/* 
            Check if there are previously made layers

            If so, load those

            Else, default to 4
        */}

        <button>
            Add Layer
        </button>
        
    </div>
    )
}